import React from "react";
import Page from "../components/common/Page";

const Claim: React.FC = () => {
  return <Page>Claim</Page>;
};

export const Component = Claim;
Component.displayName = "Claim";

export default Claim;
