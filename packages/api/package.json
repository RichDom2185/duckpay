{
  "name": "@repo/api",
  "version": "1.0.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit && eslint \"src/**/*.ts*\" --max-warnings 0"
  },
  "dependencies": {
    "@repo/database": "*",
    "@repo/payments": "*",
    "axios": "^1.7.2"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "eslint": "^8.57.0",
    "typescript": "^5.5.3"
  }
}
