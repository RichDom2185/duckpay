{
  "name": "@repo/api",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit && eslint \"src/**/*.ts*\" --max-warnings 0"
  },
  "dependencies": {
    "@repo/accounts": "workspace:*",
    "@repo/payments": "workspace:*",
    "axios": "^1.15.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "eslint": "^9.39.4",
    "typescript": "^5.5.3"
  }
}
